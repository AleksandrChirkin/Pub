extends layout

block content
  h1 Заказ № #{order._id}

  div(style='margin-left:20px;margin-top:20px')

  h4 Столики

  dl
    each _table in order.tables
      dt
        a(href=table.url) Столик №#{table.number}
      dd
        if table.name
          Название: #{table.name}

  h4 Время начала заказа: #{order.order_time}
  hr
  h4 Продолжительность: #{order.duration}
  
  p Вы точно хотите отменить этот заказ?

  form(method='POST' action='')
    div.form-group
      input#orderid.form-control(type='hidden',name='orderid', required='true', value=order._id )

    button.btn.btn-primary(type='submit') Удалить
