extends layout

block content
  h1 #{title}

  form(method='POST' action='')
    div.form-group
      label(for='id') Введите имя заказа, который Вы хотите найти:
      input#name.form-control(type='text', placeholder='Номер заказа' name='id' required='true' value='')
    button.btn.btn-primary(type='submit') Поиск
